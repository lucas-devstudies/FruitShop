import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  safe: {
    width:'100%',
    backgroundColor: '#35AB4F',
  },
  container: {
    height: 60,
    justifyContent: 'center',
    alignItems: 'center',
  },
  title: {
    color: '#fff',
    fontSize: 20,
    fontWeight: 'bold',
    letterSpacing: 2,
  },
});