import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    touchCard:{
    alignItems:'center',
    display:'flex',
    height:200,
  },
  card: {
    width:60,
    height:60,
    alignItems:'center',
    justifyContent:'center',
    borderRadius: 300,
    marginBottom: 10,
  }
});